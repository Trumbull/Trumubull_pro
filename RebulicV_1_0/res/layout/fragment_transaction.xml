<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ru.trumbull_pro.rebulicv_1_0.fragment.Transaction" >

 <TextView
     android:id="@+id/volume_transaction"
     android:layout_width="100dp"
     android:layout_height="32dp"
     android:layout_alignBaseline="@+id/quotes"
     android:layout_alignBottom="@+id/quotes"
     android:layout_toRightOf="@+id/quotes"
     android:gravity="left"
     android:textSize="20dip" />

 <TextView
     android:id="@+id/price_transaction"
     android:layout_width="100dp"
     android:layout_height="32dp"
     android:layout_alignLeft="@+id/quotes"
     android:layout_below="@+id/quotes"
     android:gravity="left"
     android:textSize="20dip" />

 <TextView
     android:id="@+id/quotes"
     android:layout_width="100dp"
     android:layout_height="32dp"
     android:layout_alignParentLeft="true"
     android:layout_alignParentTop="true"
     android:gravity="left"
     android:textSize="20dip" />

</RelativeLayout>
